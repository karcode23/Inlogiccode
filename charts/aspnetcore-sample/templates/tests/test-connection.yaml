apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aspnetcore-sample.fullname" . }}-test-connection"
  labels:
    {{- include "aspnetcore-sample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aspnetcore-sample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
